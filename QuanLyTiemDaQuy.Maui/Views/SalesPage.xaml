<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="QuanLyTiemDaQuy.Maui.Views.SalesPage"
             Title="Bán hàng">

    <RefreshView IsRefreshing="{Binding IsRefreshing}"
                 Command="{Binding RefreshCommand}">
        <ScrollView>
            <VerticalStackLayout Padding="16" Spacing="16">
                
                <!-- Recent Invoices -->
                <Label Text="Hóa đơn gần đây" Style="{StaticResource Subtitle}"/>
                
                <CollectionView ItemsSource="{Binding Invoices}"
                                HeightRequest="300">
                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <Frame Margin="0,4" Padding="12" Style="{StaticResource Card}">
                                <Grid ColumnDefinitions="*,Auto">
                                    <VerticalStackLayout>
                                        <Label Text="{Binding InvoiceCode}" FontAttributes="Bold"/>
                                        <Label Text="{Binding CustomerName}" Style="{StaticResource Caption}"/>
                                        <Label Text="{Binding InvoiceDate, StringFormat='{0:dd/MM/yyyy HH:mm}'}" 
                                               Style="{StaticResource Caption}"/>
                                    </VerticalStackLayout>
                                    <VerticalStackLayout Grid.Column="1" HorizontalOptions="End">
                                        <Label Text="{Binding Total, StringFormat='{0:N0} ₫'}"
                                               FontAttributes="Bold"
                                               TextColor="{StaticResource Success}"/>
                                        <Label Text="{Binding Status}" 
                                               Style="{StaticResource Caption}"
                                               HorizontalTextAlignment="End"/>
                                    </VerticalStackLayout>
                                </Grid>
                            </Frame>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>

                    <CollectionView.EmptyView>
                        <VerticalStackLayout VerticalOptions="Center" 
                                             HorizontalOptions="Center"
                                             Padding="32">
                            <Label Text="🧾" FontSize="48" HorizontalTextAlignment="Center"/>
                            <Label Text="Chưa có hóa đơn" HorizontalTextAlignment="Center"/>
                        </VerticalStackLayout>
                    </CollectionView.EmptyView>
                </CollectionView>

                <!-- Quick Actions -->
                <Label Text="Thao tác nhanh" Style="{StaticResource Subtitle}"/>
                
                <Grid ColumnDefinitions="*,*" ColumnSpacing="12">
                    <Button Grid.Column="0" 
                            Text="➕ Tạo hóa đơn mới"
                            Style="{StaticResource PrimaryButton}"/>
                    <Button Grid.Column="1" 
                            Text="📋 Xem tất cả"
                            Style="{StaticResource SecondaryButton}"/>
                </Grid>

                <ActivityIndicator IsRunning="{Binding IsLoading}"
                                   IsVisible="{Binding IsLoading}"
                                   HorizontalOptions="Center"/>
            </VerticalStackLayout>
        </ScrollView>
    </RefreshView>

</ContentPage>
