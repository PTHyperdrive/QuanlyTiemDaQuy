<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:QuanLyTiemDaQuy.Maui.ViewModels"
             x:Class="QuanLyTiemDaQuy.Maui.Views.LoginPage"
             Shell.NavBarIsVisible="False"
             BackgroundColor="{StaticResource Primary}">

    <ScrollView>
        <VerticalStackLayout 
            Padding="32"
            Spacing="24"
            VerticalOptions="Center"
            MaximumWidthRequest="400">

            <!-- Logo/Title -->
            <VerticalStackLayout Spacing="8" HorizontalOptions="Center">
                <Label Text="💎" 
                       FontSize="64" 
                       HorizontalTextAlignment="Center"/>
                <Label Text="Quản Lý Tiệm Đá Quý" 
                       Style="{StaticResource Title}"
                       TextColor="White"
                       HorizontalTextAlignment="Center"/>
                <Label Text="Hệ thống POS di động" 
                       Style="{StaticResource Caption}"
                       TextColor="{StaticResource Gray200}"
                       HorizontalTextAlignment="Center"/>
            </VerticalStackLayout>

            <!-- Login Card -->
            <Frame Style="{StaticResource Card}" 
                   Padding="24"
                   CornerRadius="16">
                <VerticalStackLayout Spacing="20">
                    
                    <Label Text="Đăng nhập" 
                           Style="{StaticResource Subtitle}"
                           HorizontalTextAlignment="Center"/>

                    <!-- Username -->
                    <VerticalStackLayout Spacing="6">
                        <Label Text="Tên đăng nhập" Style="{StaticResource Caption}"/>
                        <Border Stroke="{StaticResource Gray200}" 
                                StrokeThickness="1" 
                                StrokeShape="RoundRectangle 8"
                                BackgroundColor="White"
                                Padding="12,0">
                            <Entry x:Name="UsernameEntry"
                                   Placeholder="Nhập tên đăng nhập"
                                   Text="{Binding Username}"
                                   ReturnType="Next"/>
                        </Border>
                    </VerticalStackLayout>

                    <!-- Password -->
                    <VerticalStackLayout Spacing="6">
                        <Label Text="Mật khẩu" Style="{StaticResource Caption}"/>
                        <Border Stroke="{StaticResource Gray200}" 
                                StrokeThickness="1" 
                                StrokeShape="RoundRectangle 8"
                                BackgroundColor="White"
                                Padding="12,0">
                            <Entry x:Name="PasswordEntry"
                                   Placeholder="Nhập mật khẩu"
                                   Text="{Binding Password}"
                                   IsPassword="True"
                                   ReturnType="Done"/>
                        </Border>
                    </VerticalStackLayout>

                    <!-- Branch Selection -->
                    <VerticalStackLayout Spacing="6">
                        <Label Text="Chi nhánh" Style="{StaticResource Caption}"/>
                        <Border Stroke="{StaticResource Gray200}" 
                                StrokeThickness="1" 
                                StrokeShape="RoundRectangle 8"
                                BackgroundColor="White"
                                Padding="8,4"
                                HeightRequest="44">
                            <Picker x:Name="BranchPicker"
                                    ItemsSource="{Binding Branches}"
                                    ItemDisplayBinding="{Binding Name}"
                                    SelectedIndex="{Binding SelectedBranchIndex, Mode=TwoWay}"
                                    TextColor="{StaticResource TextPrimary}"
                                    HorizontalOptions="Fill"
                                    VerticalOptions="Center"/>
                        </Border>
                    </VerticalStackLayout>

                    <!-- Error Message -->
                    <Label Text="{Binding ErrorMessage}"
                           TextColor="{StaticResource Error}"
                           IsVisible="{Binding HasError}"
                           HorizontalTextAlignment="Center"
                           FontSize="12"/>

                    <!-- Login Button -->
                    <Button Text="Đăng nhập"
                            Style="{StaticResource PrimaryButton}"
                            Command="{Binding LoginCommand}"
                            IsEnabled="{Binding IsLoading, Converter={StaticResource InvertedBoolConverter}}"
                            HeightRequest="50"/>

                    <!-- Loading Indicator -->
                    <ActivityIndicator IsRunning="{Binding IsLoading}"
                                       IsVisible="{Binding IsLoading}"
                                       HorizontalOptions="Center"/>
                </VerticalStackLayout>
            </Frame>

            <!-- Footer -->
            <Label Text="© 2024 Quản Lý Tiệm Đá Quý"
                   Style="{StaticResource Caption}"
                   TextColor="{StaticResource Gray300}"
                   HorizontalTextAlignment="Center"/>

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
